<?xml version="1.0" encoding="utf-8"?>
<cruise xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="cruise-config.xsd" schemaVersion="90">
  <server artifactsdir="artifacts" siteUrl="${siteurl}" secureSiteUrl="${securesiteurl}" agentAutoRegisterKey="${autoregister}" commandRepositoryLocation="default" serverId="${serverid}">
    <!-- our little container expects a single ldap uri with a single base url and filter -->
     <!-- CONFIGLDAP -->
  </server>
  <config-repos>
  <!-- CONFIGREPOS -->
  </config-repos>
</cruise>